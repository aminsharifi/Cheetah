@page "/AI"
@attribute [Authorize]
@inject IConfiguration Configuration
@if (bool.Parse(@Configuration["AI:Enable"]!))
{
            <PageTitle>دستیار هوشمند</PageTitle>
            <AIChat UserGuideId=0></AIChat>
}
@code {

}