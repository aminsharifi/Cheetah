@inherits CUpsert_GeneralCode

<CEditDimentions Record="@Record" Records="@Records"
                 ParentCallback="@LoadDTO" reference="@reference"
                 Name="@Name" Title="@Title" IsLoading="@IsLoading"
                 IsInline="@IsInline" Id="@Id" IsUpdatedLink="@false">
</CEditDimentions>

@code {

}